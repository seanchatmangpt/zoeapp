// Auto-generated by ggen specta-exporter
use serde::{Serialize, Deserialize};
use specta::Type;

#[derive(Serialize, Deserialize, Type, Debug, Clone)]
pub struct <%= structName %> {
<% fields.forEach(field => { %>
    pub <%= field.name %>: <%= field.rustType %>,
<% }); %>
}

pub fn export_specta_types() {
    specta::export::ts(
        "<%= outputPath %>",
        &[<%= structName %>::export_type()],
    ).unwrap();
    
    // Register as a reality-receipt artifact
    // Reality Receipt Binding: <%= structName %> -> <%= outputPath %>
}
